wifi = dict(
    ssid = "ssid",
    key = "key",
    host = "hostname"
)

hass = dict(
    url = "http://localhost",
    token = "<token>"
)

motion_sensor = dict(
    friendly_name = "Test Motion",
    name = "test_motion",
    timeout_ms = 900_000,
    pin = 22
)

